[mysqld]
datadir         = /mnt/dbraid/mysql
bind-address    = 192.168.200.5

# ── Configuración de Replicación (Maestro) ──
server-id       = 1
log_bin         = /var/log/mysql/mysql-bin.log
binlog_do_db    = chat_db
expire_logs_days = 7
max_binlog_size  = 100M
